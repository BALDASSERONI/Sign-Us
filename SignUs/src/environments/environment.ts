// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  firebase: {
    projectId: 'signus-66f22',
    appId: '1:27705039499:web:c99f89e2e231a94314928e',
    storageBucket: 'signus-66f22.appspot.com',
    locationId: 'us-central',
    apiKey: 'AIzaSyCPiTXEKPOMmxdEpb0HBD4s5ucxe-c1_1s',
    authDomain: 'signus-66f22.firebaseapp.com',
    messagingSenderId: '27705039499',
    measurementId: 'G-KVCVQMLBN3',
  },
  production: false
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
